class Track {
  constructor(id, title, album, artist, seconds, explicit, imageUrl) {
    this.id = id;
    this.title = title;
    this.album = album;
    this.artist = artist;
    this.seconds = seconds;
    this.explicit = explicit;
    this.imageUrl = imageUrl;
  }
}

export default Track;
